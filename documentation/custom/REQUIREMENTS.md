# 用户需求与验收清单

更新日期：2026-09-24。本文区分用户需求、当前源码状态和实际验证结果。复选框只在对应检查实际运行并通过后勾选；“源码已编写”不等于编译、模拟器或真机通过。运行证据见 [VALIDATION.md](VALIDATION.md)。

## 用户需求

| 编号 | 需求 | 当前状态 |
| --- | --- | --- |
| R1 | 获取 Xtreme，看看功能和可升级之处 | 主源码与初步评估已完成；Xtreme 仅作参考 |
| R2 | 找功能接近、仍有更新的固件 | 已比较，维护状态记录截至 2026-09-10 |
| R3 | 使用 Momentum，延续风格并加入 Unleashed 的功能 | 已实现首批三项适配；不等于完整合并 Unleashed |
| R4 | 看看 Flipper Zero 还能怎样加强实际使用 | 通过 R8–R10 落实；其余建议见文末候选项 |
| R5 | 考虑 ESP32 和“WiFi 终结者”加入后的调整升级 | 型号、固件和接线未确认；只保留需求，不编写驱动 |
| R6 | （历史）先不用全部做，把要求和成果上传个人 GitHub | 上一轮的交付范围，已被 R11 取代 |
| R7 | 换另一台电脑继续 | 交接、依赖锁定和恢复步骤见 [HANDOFF.md](../../HANDOFF.md) |
| R8 | 新做手机软件，连接 Flipper 并利用手机算力增强功能 | iPhone 优先，目标设备 iPhone 17 Pro Max；源码部分已编写、部分编写中，从未编译 |
| R9 | Flipper 本机功能也要重写 | 部分重写：新增中文入口应用 Flipper Lab，保留已验证的硬件驱动和现有应用；编写中 |
| R10 | 提供中文版，并附带功能介绍 | 手机界面与离线指南为中文；设备端由 Flipper Lab 提供中文入口和说明；现有原生应用未翻译 |
| R11 | 实现仓库中的全部需求，不停留在方案 | 当前要求；按下方范围推进，未验证项保持未勾选 |

## 当前实施范围

设计依据见 [IPHONE_ZH_PLAN.md](IPHONE_ZH_PLAN.md)（保留为设计与历史记录）。功能的中文说明见 [FEATURE_CATALOG.zh-CN.json](FEATURE_CATALOG.zh-CN.json)，App 内置副本必须与其逐字节一致。

### iPhone App（`mobile/FlipperLab`）

| 模块 | 源码状态 |
| --- | --- |
| RPC 分帧与 protobuf 解码（仅用 Foundation，限制帧长和字段数） | 已编写；单元测试源码已编写，未运行 |
| Core Bluetooth：搜索 0x3080 广播，配对后使用串口服务，按设备流控额度分块发送，命令逐条执行 | 已编写 |
| 协议检查（要求 RPC 0.25 起的 0.x 版本）与设备信息 | 已编写 |
| 超时与取消：单条命令 45 秒无数据或总计 10 分钟即断开；取消也断开连接 | 已编写 |
| 列目录、下载；上传使用唯一 ASCII 文件名 `Lab_<UUID>` 并读回校验 | 已编写 |
| 红外：核对设备文件与手机记录一致后，按按钮序号发送一次 | 已编写 |
| 资料库：导入、编辑名称/标签/备注、只删除手机记录；任务记录只保留本次运行 | AppModel 已编写；存储实现编写中 |
| 六类记录解析（红外、Sub-GHz、NFC、低频 RFID、iButton、串口文本）及测试 | 编写中 |
| 五个中文原生页面 | 编写中 |
| Xcode 工程、`mobile/README.md` 与 GitHub macOS CI | 编写中 |
| 中文离线指南 | 目录内容与加载代码已编写 |

### Flipper 固件

| 模块 | 源码状态 |
| --- | --- |
| Flipper Lab（`applications/main/lab`）：中文入口菜单、分页功能说明、文泉驿 12 像素字体子集，经加载器队列启动现有应用 | 编写中 |
| TextBox 等文字换行的 UTF-8 正确性修复 | 编写中 |
| 现有原生应用与系统设置 | 保持原有语言，本轮不翻译 |
| 硬件驱动 | 保留已验证实现，不重写 |

### 边界

- 记录分析只在手机本地进行；单个文件不超过 2 MiB，脉冲图最多 512 个样本，资料库上限 1000 条、约 32 MiB。
- App 不含云端 AI 服务；本机 Claude 只是开发工具。
- 不新增密钥恢复、滚动码复现、实时射频数据流或未确认扩展板的驱动。
- App 不含固件升级功能；本轮不刷写设备、不部署、不发布 App Store。

## 验收清单

### 构建基线

- [x] 全部固定子模块递归初始化，未改变 gitlink。
- [x] Windows 核心构建 `SKIP_EXTERNAL=1 updater_package` 退出码 0，生成更新包和 SDK。
- [ ] 完整更新包（含外部应用）构建成功并记录产物。
- [ ] 记录更新包 SHA-256。
- [ ] 加入 Flipper Lab、字库和 UTF-8 修复后重新构建，记录固件大小及剩余 Flash/RAM。
- [ ] 在有桌面 C 编译器的环境运行旧回归，3 项真正通过（Windows 当前为 1 通过、2 跳过）。
- [ ] 核对新增 API 导出与已有应用的兼容性。

### iPhone App

- [ ] FlipperCore 单元测试（RPC、解析、存储）在 macOS 上运行通过。
- [ ] App 在 Xcode 或 GitHub macOS CI 中编译通过。
- [ ] 模拟器中五个页面可用，指南正常加载，文字无截断。
- [ ] 六类记录的固定样本：正常、空文件、损坏文件、超过 2 MiB 均有正确结果或提示。
- [ ] 资料库达到 1000 条或 32 MiB 上限、重启 App、存储损坏时行为正确，原文件不丢失。
- [ ] iPhone 17 Pro Max 真机：拒绝蓝牙权限、蓝牙关闭、首次配对、设备忙碌、取消均显示正确状态。
- [ ] 完整蓝牙流程真机跑通：搜索 → 配对 → 协议版本与设备信息 → 列目录 → 下载 → 上传并读回 → 红外发送一次 → 断开；记录至少 20 次重连结果。
- [ ] 红外发送后自有家电实际响应。

### Flipper 本机

- [ ] Flipper Lab 与字库编译进固件。
- [ ] 真机屏幕检查：中文无乱码、无截断，翻页、返回键和启动现有应用正常。
- [ ] UTF-8 换行修复的回归测试通过，英文界面不受影响。
- [ ] 记录字库来源与许可证。
- [ ] 现有原生应用和系统设置的全面中文化（当前未覆盖，另行评估）。
- [ ] 刷写真机后验证首次/再次启动带资源应用、主题切换、SD 卡失败和插件损坏场景。

### 扩展硬件

- [ ] 确认 ESP32、“WiFi 终结者”及其他扩展板的准确型号、固件、接线和供电。
- [ ] 硬件确认后再定义诊断功能与验收；在此之前不提供操作入口。

## 仍需确认的信息

| 项目 | 需要记录的信息 |
| --- | --- |
| iPhone | 已知为 iPhone 17 Pro Max；确切 iOS 版本未记录 |
| Mac | 型号、macOS 与 Xcode 版本（用于本地编译、签名和上机） |
| Flipper Zero | 硬件版本、当前固件/API、SD 卡、主要使用场景 |
| ESP32 板卡 | 精确型号、芯片、Flash/PSRAM、接线与电源、固件版本 |
| “WiFi 终结者” | 产品名/项目链接、板卡型号、当前固件、与 ESP32 是否同一设备 |
| 其他模块 | 型号、接口、电压、电源及现有应用 |

“WiFi 终结者”可能对应不同设备或项目，不能默认等同于某个 Marauder、ESP32 型号或特定软件。本轮未增加或测试 Wi-Fi 功能。

## 历史与候选

### 首批固件改动（上一轮）

- [x] 应用内置资源释放时显示进度，保留 Momentum 加载流程与主题行为。
- [x] 插件扫描遇到失败继续扫描，无线驱动采用 `radio_device_*.fal` 筛选。
- [x] 源文件模式保持原顺序去重，不随 Python 哈希种子变化。
- [x] 三个桌面回归、格式检查及差异空白检查通过（上一台 macOS 的历史结果）。

真机验证仍在上面的“Flipper 本机”清单中，未完成前不能描述为可实际使用。

### 软件候选项

这些建议最初来自 Xtreme 代码抽样阅读，不是用户确认的需求；是否纳入由主控决定，实施前先核对 Momentum 当前实现。

| 候选方向 | 当前情况 |
| --- | --- |
| 文件搜索范围、类型过滤、分页 | 未开始；先测量大目录耗时与内存 |
| 设置保存失败反馈和恢复 | 未开始；先核对 Momentum 写入链路 |
| 中文菜单与帮助 | 由 Flipper Lab 部分覆盖，编写中 |
| 红外遥控整理 | 由 iPhone 红外工作台覆盖，编写中 |
| 串口日志/GPIO 工具 | 只支持导入已保存的日志；实时采集未实现 |
| 12 小时时间格式边界 | 未核查 Momentum 是否仍有 Xtreme 的正午 AM 问题 |
| 持续跟进上游 | 逐项比对 Momentum/Unleashed 提交、API 与回归 |
